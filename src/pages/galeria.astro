---
import Layout from "@/layouts/Layout.astro";

import { getPageGaleria } from "@/lib/wp";
const { pageTitle, subtitle, image } = await getPageGaleria();
---

<Layout title={pageTitle} subtitle={subtitle} bgImage={image.url}>

</Layout>
